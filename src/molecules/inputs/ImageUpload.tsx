'use client';

import IImageUploadProps from './interfaces/imageUploadProps.interface';

const ImageUpload: React.FC<IImageUploadProps> = ({ onChange, value }) => {
	const handleUpload = () => {};

	return <div>Hello am ImageUpload</div>;
};

export default ImageUpload;
